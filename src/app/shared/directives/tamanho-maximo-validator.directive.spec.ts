import { TamanhoMaximoValidatorDirective } from './tamanho-maximo-validator.directive';

describe('TamanhoMaximoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanhoMaximoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
